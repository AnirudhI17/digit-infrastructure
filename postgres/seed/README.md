# Seed Data

This folder stores mock datasets and SQL insert scripts required to initialize the local DIGIT environment.

## Usage
Add SQL files or CSV scripts here to populate initial core records (e.g. users, default tenants, localization keys, MDMS schemas) for local debugging and development.
